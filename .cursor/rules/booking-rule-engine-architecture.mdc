---
description: 預約系統核心架構—資源抽象、規則引擎、時段計算、狀態機、多租戶與後台規格
alwaysApply: true
---

# 預約系統：可重用與可維護（規則引擎思維）

核心原則：**避免硬編碼業務名詞與流程**，改以**可設定規則 + 可插拔策略**擴充；新增業種應盡量「加類型／加策略／加資料」，而非改核心預約流程。

## 1. 資源抽象化（Resource Abstraction）

- 核心模型統一為 **Resource**（可預約實體）與 **Slot**（時間容量單位），不要將網域寫死成「訂位」「掛號」等專用詞貫穿全層。
- 用 **ResourceType** 與 **Metadata**（JSON／JSONB 等）承載差異屬性（房間、醫師、車輛、課程共用同一套預約管線）。
- 新業務優先：**新增類型與 metadata schema**，而非複製一套預約 API。

## 2. 策略模式：預約規則（Strategy）

- 定義 **`BookingValidationStrategy`**（或同等介面），依資源類型掛不同實作（例：醫療掛號費上限、場地人數上限、設備維修資格）。
- 透過 **Spring 註冊多個 Bean**，執行時依 `ResourceType`（或鍵值）**選擇策略**，避免在 Service 內用長串 `if/else`。
- 新增規則 = **新實作類 + 註冊**，不修改既有策略類別。

## 3. 時段引擎：計算優先於大量預存（Slot Engine）

- DB 存 **營業／開放規則**（如週一至五 09:00–18:00）與 **Exceptions**（假日、臨時關閉、特殊時段）。
- **不要**預先展開未來全年每日 Slot 列（資料膨脹、規則變更難回補）。
- 查詢可用時段時由 **Slot Engine 即時計算**，再與 **已預約／鎖定** 資料取差集或交集產生結果；管理者改營業時間應**立即生效**。

## 4. 狀態機（State Machine）

- 預約生命週期（例：待支付 → 已確認 → 使用中 → 已完結；分支：取消、逾期）須有**明確轉移表**。
- 使用 **Spring Statemachine、自建狀態機服務、或嚴格 Enum + 集中 transition 方法**；禁止在任意層「直接改 status 欄位」繞過規則。
- 轉移條件寫清楚（例：僅 **Confirmed** 可 **Cancel**），並在單元測試覆蓋非法轉移。

## 5. 多租戶（Multi-tenancy）

- 租戶相關表帶 **`tenant_id`**；所有查寫預設帶租戶範圍。
- 若採 **MyBatis-Flex**：用 **Tenant 攔截器** 自動注入條件。若目前為 **JPA**：以 **Filter、Specification、或 Repository 強制規範** 達成同等「無 tenant 不查」的保證。
- API／背景任務一律從安全上下文解析 tenant，避免遺漏。

## 管理者後台規格（Maintainability）

| 能力 | 要點 |
|------|------|
| **A. 通知觸發器** | 管理者可設定觸發點（預約成功、活動前 N 小時等）；技術上可接 **SNS/Lambda、佇列、或延遲隊列（如 Redis）**，避免為每種通知硬寫 Cron。 |
| **B. 批次／連鎖修改** | 如「一鍵關閉未來一週」：以 **Exceptions 範圍記錄**（高優先於常規開放規則）驅動 Slot Engine，而非逐筆刪改萬筆 slot。 |
| **C. 審計日誌** | 誰、何時、改了資源價格或取消預約等：**AOP 攔截管理端關鍵操作**，寫入稽核儲存（表或專用 log）。 |

## 實作檢查（給 Agent）

- 新功能先問：是否屬於 **Resource/Slot 通用模型**？若否，是否應進 **Metadata 或策略**？
- 時段相關變更是否走 **規則 + Engine**，而非手動維護大量預先展開列？
- 狀態變更是否**唯一入口**？多租戶條件是否**無法被略過**？
