---
description: 規範 doc/specs 規格文件命名格式
globs: doc/specs/**/*.md
alwaysApply: true
---

# Spec Naming Convention

`doc/specs/open/`、`doc/specs/progress/` 與 `doc/specs/closed/` 下的規格文件，檔名一律使用：

- `YYYY-MM-DD_<kebab-case-topic>.md`

## 狀態資料夾

- **擬新增（預設起點）**：一律先建立在 `doc/specs/open/`。
- **進行中（已開工）**：使用 `doc/specs/progress/`。
- **已結案**：移至 `doc/specs/closed/`（檔名不變）。流程見 `spec-done-archive.mdc`。

## Rules

- 必須以日期前綴（`YYYY-MM-DD`）開頭。
- 日期後使用底線 `_` 連接主題。
- 主題使用 `kebab-case`（小寫英文與連字號）。
- 不可使用無日期前綴的檔名（例如 `unified-login-workplan.md`）。

## Examples

- `doc/specs/open/2026-04-10_frontend-driven-backend-api-requirements.md`（新提案）
- `doc/specs/progress/2026-04-10_user-can-be-merchant-mvp.md`（進行中）
- `doc/specs/closed/2026-04-09_home-intro-page-redesign.md`（已結案）
- `doc/specs/closed/2026-04-09_visual-assets-dashboard-guidelines.md`（已結案）
- `doc/specs/closed/2026-04-02_booking-flow-pm-spec.md`

