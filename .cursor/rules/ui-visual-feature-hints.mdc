---
description: 使用者介面多用插圖與視覺元素提示功能
globs: frontend/**/*.{jsx,js}
alwaysApply: false
---

# 用「有趣的圖」提示功能

在可視區域優先用**插圖、圖示、情境小圖**輔助說明，讓使用者一眼理解能做什麼，而不只靠長文字。

## 建議做法

- **空狀態（empty state）**：不要只顯示「尚無資料」；搭配與情境相關的插圖或輕量 SVG，並用一句話說明下一步（例如「建立第一筆預約」+ 小日曆圖）。
- **功能區塊標題**：重要模組旁放**語意清楚的圖示**或**小 banner 圖**（與該功能強相關），避免整頁只有標題與表單。
- **引導與首次使用**：新功能可用**步驟示意圖**或**簡短漫畫式插畫**（一屏一重點），減少說明文字牆。
- **錯誤與成功**：在 toast / 內嵌訊息中可搭配**小型狀態插圖**（成功打勾、搜尋無結果的放大鏡空景），語氣維持友善。
- **一致性**：圖風格（線條粗細、色票、圓角）與現有設計系統對齊；優先**內嵌 SVG 或可快取的 WebP**，注意 `alt` 與對比度（WCAG）。

## 避免

- 裝飾性強、與功能無關的圖（干擾任務）。
- 過大動畫或閃爍（造成疲勞或無障礙問題）。
- 僅用 emoji 取代語意圖示時，在不同平台顯示不一致——重要操作仍用**自訂圖示或插圖**。

## 簡例（概念）

```jsx
// ❌ 僅文字
<p>您還沒有任何預約</p>

// ✅ 圖 + 短文案 + 主行動
<EmptyState
  illustration={<CalendarIllustration />}
  title="從這裡開始管理預約"
  description="建立時段後，顧客就能線上選時間。"
  primaryAction={{ label: "前往設定時段", to: "/merchant/schedule" }}
/>
```

優先在有**空狀態、 onboarding、儀表板區塊、錯誤頁**的地方補上與功能連結的視覺提示。
