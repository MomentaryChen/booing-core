---
description: 按鈕圖示、空狀態插圖、資產格式與一致視覺語言
globs: frontend/**/*.{jsx,js}
alwaysApply: false
---

# UI 視覺規則

## 必須遵守

- **所有按鈕須有圖示**：文字按鈕搭配**語意清楚的 leading 或 trailing icon**（與 shadcn `Button` 的 `asChild` + `lucide-react` 等一致）；**純圖示按鈕**必須有無障礙標籤（例如 `aria-label` / `title`）。例外僅限極少數既有設計系統已定義的變體，且須在 PR 註明理由。
- **空狀態使用插圖**：empty state 不可只有一句「尚無資料」；須有**與情境相關的插圖**（自訂或設計系統內 SVG），外加短標題、一句說明與主行動。
- **禁止庫存照片**：不要用一般 stock photo 當裝飾或空狀態主視覺；改用手繪／扁平插畫、圖示化情境圖或產品內 SVG。
- **向量優先於點陣**：**優先 SVG**（內嵌或 sprite）；需點陣時再用 WebP 等；**避免 PNG/JPEG** 當 UI 圖示或小型裝飾（除非既有資產或第三方限制）。
- **一致視覺語言**：線條粗細、色票、圓角、陰影與現有儀表板／shadcn 風格對齊；新插圖與圖示沿用同一套規則。

# 用「有趣的圖」提示功能

在可視區域優先用**插圖、圖示、情境小圖**輔助說明，讓使用者一眼理解能做什麼，而不只靠長文字。

## 建議做法

- **功能區塊標題**：重要模組旁放**語意清楚的圖示**或**小 banner 圖**（與該功能強相關），避免整頁只有標題與表單。
- **引導與首次使用**：新功能可用**步驟示意圖**或**簡短漫畫式插畫**（一屏一重點），減少說明文字牆。
- **錯誤與成功**：在 toast / 內嵌訊息中可搭配**小型狀態插圖**（成功打勾、搜尋無結果的放大鏡空景），語氣維持友善。
- **可及性**：圖與插圖須有適當 `alt`／裝飾性則 `aria-hidden`；對比度符合 WCAG。

## 避免

- 裝飾性強、與功能無關的圖（干擾任務）。
- 過大動畫或閃爍（造成疲勞或無障礙問題）。
- 僅用 emoji 取代語意圖示時，在不同平台顯示不一致——重要操作仍用**自訂圖示或插圖**。

## 簡例（概念）

```jsx
// ❌ 僅文字
<p>您還沒有任何預約</p>

// ✅ 圖 + 短文案 + 主行動（主行動按鈕須含圖示）
<EmptyState
  illustration={<CalendarIllustration />}
  title="從這裡開始管理預約"
  description="建立時段後，顧客就能線上選時間。"
  primaryAction={{ label: "前往設定時段", icon: <CalendarIcon />, to: "/merchant/schedule" }}
/>
```

優先在有**空狀態、 onboarding、儀表板區塊、錯誤頁**的地方補上與功能連結的視覺提示。
