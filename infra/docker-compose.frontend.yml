services:
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE: ${FRONTEND_VITE_API_BASE:-/api}
    container_name: booking-core-frontend-only
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-18080}:80"
